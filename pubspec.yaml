name: easysupport_sdk
description: "Easy Support Flutter SDK with channel bootstrap, customer session, and real-time chat."
version: 0.0.4+1
repository: https://github.com/kamran-octet/easysupport_sdk

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.1
  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.3
  socket_io_client: ^3.1.4
  web_socket_channel: ^3.0.3
  webview_flutter: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
